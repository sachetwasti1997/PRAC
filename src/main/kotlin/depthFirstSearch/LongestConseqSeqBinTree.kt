package depthFirstSearch

import tree.TreeNode

class LongestConseqSeqBinTree {
  fun find(root: TreeNode?, dir: Int, ln: Int): Int {
    return 1
  }
  fun longestConsecutive(root: TreeNode?): Int {
    return 1
  }
}