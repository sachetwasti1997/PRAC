package arrays

class SortAnArray {
  /*
  fun sortArray(nums: IntArray): IntArray {
    val pq = PriorityQueue(nums.size)
    for (i in nums) {
      pq.add(i)
    }
    val res = ArrayList<Int>()
    while (!pq.isEmpty()) {
      res.add(pq.poll())
    }
    return res.toIntArray()
  }
   */
  fun sortArray(nums: IntArray): IntArray {
    return intArrayOf()
  }
}