package graph

class AccountMerge {
  /*
  typealias hmsi = HashMap<String, Int>
  fun union(par: hmsi, rank: hmsi) {}
  fun accountsMerge(accounts: List<List<String>>): List<List<String>> {
    val par = hmsi(accounts.size).apply{
      for (i in accounts.indices) {
        var j = 0
        while (j < accounts[i].size) {
          if (j >= 1) this[accounts[i][j]] = i
          j++
        }
      }
    }

    val rank = hmsi(accounts.size).apply {
      for (i in accounts.indices) {
        var j = 0
        while (j < accounts[i].size) {
          if (j >= 1) this[accounts[i][j]] = 1
          j++
        }
      }
    }


  }
   */
  fun accountsMerge(accounts: List<List<String>>): List<List<String>> {
    return emptyList()
  }
}