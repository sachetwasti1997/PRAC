package matrix

class RotateImage {
  fun rotate(matrix: Array<IntArray>): Unit {
    /*
    val nw = Array(matrix.size){IntArray(matrix[0].size)}
    var i = 0
    var j = matrix[0].size-1
    while (i < matrix.size) {
      var j1 = 0
      var i2 = 0
      while (j1 < matrix[0].size) {
        nw[i2][j] = matrix[i][j1]
        i2++
        j1++
      }
      i++
      j--
    }
    for (i in matrix.indices) {
      for (j in matrix[0].indices) {
        matrix[i][j] = nw[i][j]
      }
    }
     */
  }
}