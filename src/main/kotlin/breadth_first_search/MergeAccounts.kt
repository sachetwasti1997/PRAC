package breadth_first_search

class MergeAccounts {
  /*
  class Acc(nm: String, acc: ArrayList<String> = ArrayList())
  fun accountsMerge(accounts: List<List<String>>): List<List<String>> {
    val map = HashMap<String, ArrayList<String>>(accounts.size).apply {
      for (i in accounts) {
        val a = Acc(i[0])
        var j = 1
        while (j < i.size) {
          map[i[j]]?.let {
            it.acc.add
          }
        }
      }
    }

  }
   */
}